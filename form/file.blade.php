<div class="custom-file">
    <input type="{{ $type }}" id="{{ $id }}" name="{{ $name }}" class="{{ $classes('custom-file-input') }}" value="{{ $value }}"{!! $attributesString !!}>
    <label class="custom-file-label" for="{{ $id }}">Dosya seç</label>
</div>